{
  "type": "object",
  "required": ["orbitAuthorization"],
  "properties": {
    "orbitAuthorization": {
      "type": "string",
      "title": "Orbit API authorization",
      "description": "Enter Bearer followed by a space and your Orbit API key from https://developer.orbitsearch.com/dashboard/keys. Required scopes: search:read and profile:read.",
      "pattern": "^Bearer sk_orb_.+$",
      "x-to": { "header": "Authorization" },
      "x-from": { "header": "x-orbit-authorization" },
      "x-order": 0
    }
  }
}
